---
id: VER-001
type: verification
status: verified
created: 2026-09-04
updated: 2026-09-04
related:
  - TASK-005
  - REQ-001-ZH
  - DES-002-ZH
---

# v0.1 new-project baseline verification

## Owner

root

## Scope

Validate a fresh, business-free project fixture through initialization,
record creation/indexing, task handoff, and local governance checks. Confirm
that initialization preserves user files and leaves protected Git/remote
actions outside the core CLI.

## Acceptance

The fresh-project fixture creates the standard governance skeleton and all
seven record types, preserves existing files, and leaves protected actions
unexecuted.

## Evidence

- `py -3 -m pytest -o addopts='' --basetemp D:\pgk-final-suite13 -ra` — full
  suite passed (86 passed, including fixture acceptance).
- `py -3 -m project_governance init --root <fixture-copy> --json` — returned
  `project_stage=requirements_discussion`, `git_state=git_not_initialized`, and
  created the standard profile without overwriting `README.txt`.
- Fixture acceptance test — created and indexed `REQ-001`, `DES-001`,
  `ADR-001`, `TASK-001`, `BUG-001`, `REVIEW-001`, and `VER-001`; updated the
  TASK handoff; final `pgk check --json` returned `ok=true`.
- `py -3 -m project_governance check --root . --json` — reports the expected
  dirty-worktree state while this uncommitted change is in progress; the clean
  fixture check returns `ok=true`.
- `py -3 -m compileall -q src tests` — passed.
- `git diff --check` — passed.
- `py -3 D:\skills\bootstrap-project-governance\scripts\validate_project_docs.py .` — 0 errors, 0 warnings (after test temporary directories were removed).

## Blockers

The shared checkout is intentionally dirty until a user-authorized commit;
this is reported by `pgk check` and is not a fixture failure.

## Next action

Review this baseline and decide whether to authorize a commit; remote
publication remains separately protected.

## Acceptance result

AC-01, AC-02, AC-04, AC-06, AC-08, AC-10, AC-11, AC-12, AC-13, and AC-14 are
validated by local fixture tests, source inspection, and the commands above.
No `REQ-*` business record is generated by `init_project`.

AC-03, AC-05, AC-07, and AC-09 remain partially validated because they depend
on user authorization, requirement acceptance, and real multi-agent Git
operations.

## Unverified boundaries

Real external Agent compliance, deployment, performance, runtime logging,
model-provider traffic, GitHub/Plane APIs, and protected Git/remote actions
remain unverified and are intentionally not executed by this validation.
The fixture does not prove user acceptance of a requirement or behavior of a
third-party Agent integration.
